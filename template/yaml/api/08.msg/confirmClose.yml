tags:
  - Dialog
  - 组件扩展
name: confirmClose
filetype: method
note: |
  关闭confirm弹窗信息<br>
  主要用于Android中的后退键关闭弹窗
warn: |
  只在Vue组件模式可用
code: |   
  // 关闭弹窗
  $api.confirmClose()
changes:
  v1.0.1:
    - '[change] 由alert.hide 改为 alertClose'