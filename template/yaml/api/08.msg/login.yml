tags:
  - Dialog
  - 组件扩展
name: login
filetype: method
note: |
  登录层
tip: |
  Vue组件模式下可用
args:
  reload:
    type: Boolean
    note: 登录后是否需要重新载入来源页，默认值：true
    required: false
  options:
    type: Function
    note: 登录成功的回调
    required: false
code: |        
  // 简单示例 
  $api.login()
  // 完整示例 
  $api.login(false,()=>{
      alert('登录成功')
  })
changes:
  v1.1.0:
    - '[feature] 新增：调用Vue组件，实现！'