tags:
  - 窗体
  - 通用
name: onload
filetype: method
note: |
  Dom加载完成
args:
  callback:
    type: Function
    note: 窗体
  inRefresh:
    type: Boolean
    note: 默认值:false,是否需要加入刷新列表中,提供给refresh使用
    required: false
return:
  type: Object
  note: $api对象
code: |        
  $api.onload(()=>{
    console.log('页面加载完成')
  })
changes:
  v1.1.0:
    - '[change] 新增"inRefresh"参数，表示是否要加入业务刷新列表中，默认值false'